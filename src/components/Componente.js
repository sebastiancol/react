import React, { Component } from "react";

class Componente extends Component {

    render() {

        return (
            <h1 className="textAlign: Center; textSize: 10px">saludo</h1>

            const consumir = await fetch('https://catfact.ninja/fact');
            const mostrar = await data.json()
            console.log(users)
        )



    }
}

export default Componente;